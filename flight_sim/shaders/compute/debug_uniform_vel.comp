#version 430 core
layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;
layout(rgba16f, binding = 1) uniform writeonly image3D velocity_img;
uniform vec3 u_vel;
void main() {
    ivec3 pos = ivec3(gl_GlobalInvocationID);
    imageStore(velocity_img, pos, vec4(u_vel, 1.0));
}
