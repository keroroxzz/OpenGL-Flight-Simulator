#version 430 core
layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;
layout(r8ui, binding = 3) uniform uimage3D solid_img;
void main() {
    ivec3 pos = ivec3(gl_GlobalInvocationID);
    imageStore(solid_img, pos, uvec4(0));
}
